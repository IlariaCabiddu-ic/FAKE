# example of values inserted on blockchain
JSON = {
    "ID" : "Value int", # identification block on blockchain
    "URL" :"string", # link of the source
    "Source" : "string", # name of the source
    "Date" : "dd-mm-yyyy", # publication date
    "Topic" : "list of String", # two levels of depth [level0,level1]
    "Social" : "Boolean", # check if we have a news from the social net, because the source is the author of the post
    "Message" : "List of float value", #[value, typos, word_freq]
    "Fact" : "float, list of BoW, most_trust_value", #da verificare questo
    "Context" : "list of values", #[value, n_ads, n_ref, author]
    "Extra" : ""
}